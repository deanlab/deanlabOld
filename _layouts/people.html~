---
layout: default
---

{% if page.imageid %}
  {% capture imageurl %}http://res.cloudinary.com/{{site.cloudinary_id}}/image/upload/{{page.image_options}}/{{page.imageid}}{% endcapture %}
{% elsif page.imageurl %}
  {% capture imageurl %}{{page.imageurl}}{% endcapture %}
{% endif %}

<ol class="container breadcrumb">
  <li><a href="/">Home</a></li>
  <li><a href="/people/">People</a></li>
  <li class="active">{{ page.name }}</li>
</ol>

<article class="media">
  <header>
    <h1>{{ page.name }}</h1>
    {% if page.title %}
    <h2>{{ page.title }}</h2>
    {% endif %}
  </header>

  {% if imageurl %}
    <a class="media-left" href="#">
      <img src="{{ imageurl }}" class="img-responsive" >
    </a>
  {% endif %}

  <div class="media-body">

        {% if page.lab %}
          <div>
            Lab: {{ page.lab }}
          </div>
        {% endif %}

        {% if page.office %}
          <div>
            Office: {{ page.office }}
          </div>
        {% endif %}

        {% if page.phone %}
          <div>
            Phone: {{ page.phone }}
          </div>
        {% endif %}

        {% if page.email %}
          <div>
            <a href="mailto:{{ page.email }}">
              <i class="fa fa-envelope"></i> {{ page.email }}
            </a>
          </div>
        {% endif %}

        {% if page.twitter %}
          <div>
            <a href="https://twitter.com/{{ page.twitter }}" >
              <i class="fa fa-twitter"></i>
              <span class="username"> {{ page.twitter }}</span>
            </a>
          </div>
        {% endif %}

        {% if page.googleplus %}
          <div>
            <a href="https://plus.google.com/{{ page.googleplus }}/posts"<>
              <i class="fa fa-google-plus"></i>
              <span class="username">google plus</span>
            </a>
          </div>
        {% endif %}



    {{ content }}
  </div>

  <footer class="text-right">
    <!-- Go to www.addthis.com/dashboard to customize your tools -->
    <div class="addthis_sharing_toolbox"></div>
  </footer>
</article>
